projects:
- name: mosslilley-agents-workshop-queue
  environments:
  - name: production
    databases:
    - name: mosslilley-queue-agents-db
      plan: basic-256mb
      region: oregon
      postgresMajorVersion: '18'
    services:
    - type: keyvalue
      name: mosslilley-queue-agents-valkey
      plan: starter
      region: oregon
      maxmemoryPolicy: noeviction
      ipAllowList: []
    - type: web
      name: mosslilley-queue-agents-web
      runtime: python
      region: oregon
      plan: starter
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.web
      healthCheckPath: /healthz
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mosslilley-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: mosslilley-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: '3.12'
    - type: worker
      name: mosslilley-queue-agents-worker
      runtime: python
      region: oregon
      plan: starter
      numInstances: 1
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.worker
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mosslilley-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: mosslilley-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: '3.12'
